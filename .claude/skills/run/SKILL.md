---
name: run
description: Build and run the Tutor MAUI app. No arguments needed.
---

When invoked:

1. Run: `dotnet run --project Tutor.MAUI -f net10.0-windows10.0.19041.0`
2. Stream the output so the user can see build progress and any errors
3. If the build fails, summarize the error and suggest a fix
